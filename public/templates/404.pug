doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Not found
        include essentials
    body
        include navbar
        .container(style="height:80vh").d-flex.align-items-center
            div.m-auto
                .display-1.fw-bold.text-center 404
                p.text-center Trang không tồn tại
                .text-center
                    button(onclick="history.back()").btn.btn-outline-light Quay đầu là bờ
                    